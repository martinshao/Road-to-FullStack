
async function setTimeout() {
  const { data } = await fetchData();
  dispatch({ type: 'initialData', paylaod: data });
}

useEffect(() => {
  async function setTimeout() {
    const { data } = await fetchData();
    dispatch({ type: 'initialData', paylaod: data });
  }
  setTimeout()
}, [input])

aioxs.get('./', param).then(

)

